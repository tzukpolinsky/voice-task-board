Convert the user's repeat phrase to an iCal RRULE. Current UTC: {current_utc}

Rules:
- rrule: the RRULE string (e.g. FREQ=WEEKLY;BYDAY=TH), or null if it doesn't repeat. Do NOT encode time-of-day.
- until: an ISO date (YYYY-MM-DD) if the phrase bounds it ("for a month", "until <date>"), else null. Compute relative to the current date.
